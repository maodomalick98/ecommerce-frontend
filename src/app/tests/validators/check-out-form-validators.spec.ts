import { WitheSpaceValidator } from '../../validators/witheSpaceValidator';

describe('CheckOutFormValidators', () => {
  it('should create an instance', () => {
    expect(new WitheSpaceValidator()).toBeTruthy();
  });
});
